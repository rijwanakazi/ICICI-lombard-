import { Derect1Directive } from './derect1.directive';

describe('Derect1Directive', () => {
  it('should create an instance', () => {
    const directive = new Derect1Directive();
    expect(directive).toBeTruthy();
  });
});
